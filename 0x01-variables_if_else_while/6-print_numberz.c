#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - a program that prints all single digit numbers of base 10.
 *
 * Return: 0
 */
int main(void)
{
	int num[] n;
	int num[] x;

	x = (n % 10);

	for (x = 0; x <= 10; x++)
	{
		putchar(num);
	}

	putchar('\n');
	return (0);
}
