#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - a program that prints all single digit numbers of base 10.
 *
 * Return: 0
 */
int main(void)
{
	int n;

	n = 0;
	putchar(n);
	{
		if (x = 0; x <= 10; x++)
		{
			putchar(n + '0');
		}


	return (0);
}
