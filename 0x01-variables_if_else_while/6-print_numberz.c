#include <stdio.h>
#include <stdlib.h>
/**
 * main - a program that prints all single digit numbers of base 10.
 *
 * Return: 0
 */
int main(void)
{
	int ch = 'i';

	i = 0;

	do {
		putchar(i);
		i++;
	} while (i < 10);

	putchar('\n');
	return (0);
}
