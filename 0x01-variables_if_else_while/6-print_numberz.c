#include <stdio.h>
#include <stdlib.h>
/**
 * main - a program that prints all single digit numbers of base 10.
 *
 * Return: 0
 */
int main(void)
{
	int num a;

	for (a = 0; a < 10; a++)
	{
		putchar(a);
	}

	putchar('\n');
	return (0);
}
