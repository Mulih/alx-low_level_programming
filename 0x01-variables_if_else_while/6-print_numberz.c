#include <stdio.h>
#include <stdlib.h>
/**
 * main - a program that prints all single digit numbers of base 10.
 *
 * Return: 0
 */
int main(void)
{
	int num = 'n';

	int num 'n' = '0';

	for (n = 0; n <= 10; n++)
	{
		if (n < 10)
		{
			putchar(num);
		}
	}

	putchar('\n');
	return (0);
}
