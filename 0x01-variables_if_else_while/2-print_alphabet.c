#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - prints the alphabet in lowercase
 *
 *  Return: (0)
 */
int main(void)
{
	char a;
	char z;
	char letter;

	for (letter = a; letter < z; a++)
		putchar(letter);

	putchar("\n");
	return (0);
}
